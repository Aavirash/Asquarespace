import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asquarespace.canvas',
  appName: 'Asquarespace Canvas',
  webDir: 'www'
};

export default config;
